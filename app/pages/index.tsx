import type { NextPage } from "next";
import { PublicLayout } from "../components/layouts";

const Home: NextPage = () => {
    return (
        <PublicLayout>
            
        </PublicLayout>
    );
};

export default Home;
